// Esquema de base de datos - ORBIT Store (reto periodo de prueba Turing-IA)
//
// Motor: PostgreSQL, provisionado con Neon (via integracion de Vercel
// Marketplace) para que la base sea la misma en local y en produccion.
// `url` usa la conexion "pooled" (pgbouncer, para runtime normal) y
// `directUrl` la conexion directa (requerida por las migraciones).

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DATABASE_URL_UNPOOLED")
}

enum Role {
  ADMIN
  USER
}

/// Usuarios del sistema. Un usuario puede tener rol ADMIN (gestiona catalogo)
/// o USER (navega y guarda favoritos).
model User {
  id        Int        @id @default(autoincrement())
  name      String
  email     String     @unique
  password  String
  role      Role       @default(USER)
  favorites Favorite[]
  createdAt DateTime   @default(now())
  updatedAt DateTime   @updatedAt

  @@map("users")
}

/// Categorias del catalogo propio (normalizadas fuera de Product para evitar
/// redundancia de strings repetidos por producto -> cumple 3FN).
model Category {
  id        Int       @id @default(autoincrement())
  name      String    @unique
  slug      String    @unique
  products  Product[]
  createdAt DateTime  @default(now())

  @@map("categories")
}

/// Productos del catalogo propio de la tienda (CRUD administrado por ADMIN,
/// lectura publica para el frontend).
model Product {
  id          Int        @id @default(autoincrement())
  name        String     @unique
  description String
  price       Float
  stock       Int        @default(0)
  imageUrl    String
  categoryId  Int
  category    Category   @relation(fields: [categoryId], references: [id])
  favorites   Favorite[]
  createdAt   DateTime   @default(now())
  updatedAt   DateTime   @updatedAt

  @@map("products")
}

/// Tabla intermedia (relacion muchos-a-muchos) entre User y Product para la
/// funcionalidad de favoritos. Se evita duplicar el mismo favorito con
/// una restriccion unique compuesta.
model Favorite {
  id        Int      @id @default(autoincrement())
  userId    Int
  productId Int
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  product   Product  @relation(fields: [productId], references: [id], onDelete: Cascade)
  createdAt DateTime @default(now())

  @@unique([userId, productId])
  @@map("favorites")
}
